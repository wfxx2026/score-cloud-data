name: Daily Score Fetch - Hybrid

permissions:
  contents: write
  pages: write
  id-token: write
  actions: write
  issues: write

on:
  repository_dispatch:
    types: [daily-fetch, cron-fetch, manual-fetch, pages-rebuild]
  
  schedule:
    - cron: '0 16 * * *'
    - cron: '30 15 * * *'
  
  workflow_dispatch:
    inputs:
      target_date:
        description: '指定日期 (YYYY-MM-DD，留空为今天)'
        required: false
        default: ''
      force_update:
        description: '强制更新（即使已存在数据）'
        type: boolean
        default: false
      notify_channel:
        description: '通知渠道'
        type: choice
        options:
          - 'none'
          - 'issue'
          - 'email'
        default: 'issue'

env:
  NODE_VERSION: '20'

jobs:
  precheck:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
      reason: ${{ steps.check.outputs.reason }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Check if external cron already ran
        id: check
        run: |
          TODAY=$(date +%Y-%m-%d)
          SUMMARY_FILE="daily-summary/${TODAY}.json"
          
          if [ -f "$SUMMARY_FILE" ]; then
            FILE_TIME=$(stat -c %Y "$SUMMARY_FILE")
            NOW=$(date +%s)
            DIFF=$((NOW - FILE_TIME))
            
            if [ $DIFF -lt 3600 ]; then
              echo "should_run=false" >> $GITHUB_OUTPUT
              echo "reason=External cron already ran ${DIFF}s ago" >> $GITHUB_OUTPUT
              echo "⏭️ 外部Cron已在1小时内执行，跳过"
              exit 0
            fi
          fi
          
          echo "should_run=true" >> $GITHUB_OUTPUT
          echo "reason=No recent data found" >> $GITHUB_OUTPUT
          echo "✅ 需要执行数据抓取"

  fetch:
    runs-on: ubuntu-latest
    needs: precheck
    if: |
      always() && 
      (github.event_name != 'schedule' || needs.precheck.outputs.should_run == 'true')
    
    outputs:
      fetch_count: ${{ steps.execute.outputs.fetch_count }}
      exceed_count: ${{ steps.execute.outputs.exceed_count }}
      trigger_source: ${{ steps.info.outputs.trigger_source }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      
      - name: Install dependencies
        run: |
          npm ci 2>/dev/null || npm install
      
      - name: Get trigger info
        id: info
        run: |
          echo "trigger_source=${{ github.event_name }}" >> $GITHUB_OUTPUT
          echo "trigger_actor=${{ github.actor }}" >> $GITHUB_OUTPUT
          
          if [ "${{ github.event_name }}" == "repository_dispatch" ]; then
            echo "trigger_source=external-cron" >> $GITHUB_OUTPUT
          fi
          
          if [ -n "${{ github.event.inputs.target_date }}" ]; then
            echo "target_date=${{ github.event.inputs.target_date }}" >> $GITHUB_OUTPUT
          else
            echo "target_date=$(date +%Y-%m-%d)" >> $GITHUB_OUTPUT
          fi
      
      - name: Execute fetch
        id: execute
        env:
          API_BASE_URL: ${{ secrets.API_BASE_URL }}
          API_PERSON_ID: ${{ secrets.API_PERSON_ID }}
          API_COOKIE: ${{ secrets.API_COOKIE }}
          EXTRA_USERS: ${{ secrets.EXTRA_USERS }}
          TARGET_DATE: ${{ steps.info.outputs.target_date }}
          FORCE_UPDATE: ${{ github.event.inputs.force_update }}
        run: |
          node scripts/fetch-all.js 2>&1 | tee fetch.log
          
          FETCH_COUNT=$(grep -oP '成功抓取: \K\d+' fetch.log || echo "0")
          EXCEED_COUNT=$(grep -oP '超额人数: \K\d+' fetch.log || echo "0")
          
          echo "fetch_count=$FETCH_COUNT" >> $GITHUB_OUTPUT
          echo "exceed_count=$EXCEED_COUNT" >> $GITHUB_OUTPUT
          
          echo "### 📊 抓取结果" >> $GITHUB_STEP_SUMMARY
          echo "- 日期: ${{ steps.info.outputs.target_date }}" >> $GITHUB_STEP_SUMMARY
          echo "- 触发源: ${{ steps.info.outputs.trigger_source }}" >> $GITHUB_STEP_SUMMARY
          echo "- 成功抓取: $FETCH_COUNT 人" >> $GITHUB_STEP_SUMMARY
          echo "- 超额人数: $EXCEED_COUNT 人" >> $GITHUB_STEP_SUMMARY
      
      - name: Generate report
        if: steps.execute.outputs.fetch_count != '0'
        run: |
          node scripts/generate-report.js \
            --date ${{ steps.info.outputs.target_date }} \
            --output reports
      
      - name: Setup Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Check for changes
        id: verify-changed-files
        run: |
          git add -A
          if git diff --staged --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "⏭️ 无数据变更"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "✅ 检测到变更"
          fi
      
      - name: Commit changes
        if: steps.verify-changed-files.outputs.changed == 'true'
        run: |
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          git commit -m "📊 Daily update: ${{ steps.info.outputs.target_date }} [${{ steps.info.outputs.trigger_source }}]
          
          - 抓取人数: ${{ steps.execute.outputs.fetch_count }}
          - 超额人数: ${{ steps.execute.outputs.exceed_count }}
          - 执行时间: $TIMESTAMP"
      
      - name: Push changes
        if: steps.verify-changed-files.outputs.changed == 'true'
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
      
      - name: Trigger Pages redeploy
        if: steps.verify-changed-files.outputs.changed == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createDispatchEvent({
              owner: context.repo.owner,
              repo: context.repo.repo,
              event_type: 'pages-rebuild'
            });
        continue-on-error: true

  notify:
    runs-on: ubuntu-latest
    needs: [fetch]
    if: always() && needs.fetch.result != 'skipped'
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Create issue on failure
        if: needs.fetch.result == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const title = `❌ 数据抓取失败 ${new Date().toISOString().split('T')[0]}`;
            const body = `## 失败详情
            
            - 触发源: ${{ needs.fetch.outputs.trigger_source }}
            - 工作流: ${{ github.workflow }}
            - 运行编号: ${{ github.run_id }}
            
            ### 可能原因
            1. API Cookie 过期
            2. 网络连接问题
            3. API 接口变更
            
            ### 手动重试
            [点击手动触发](https://github.com/${{ github.repository }}/actions/workflows/${{ github.workflow }})
            
            @${{ github.repository_owner }}`;
            
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              body,
              labels: ['bug', 'automated']
            });
      
      - name: Create exceed alert
        if: |
          needs.fetch.result == 'success' && 
          needs.fetch.outputs.exceed_count != '0'
        uses: actions/github-script@v7
        with:
          script: |
            const exceedCount = parseInt('${{ needs.fetch.outputs.exceed_count }}');
            const title = `⚠️ 今日发现 ${exceedCount} 人分数超额`;
            
            const fs = require('fs');
            const today = new Date().toISOString().split('T')[0];
            const summaryPath = `daily-summary/${today}.json`;
            
            let body = `## 超额提醒\n\n今日检测到 **${exceedCount}** 人分数超过45分限制。\n\n`;
            
            try {
              const data = JSON.parse(fs.readFileSync(summaryPath, 'utf8'));
              const exceeds = Object.entries(data.users)
                .filter(([_, u]) => u.isExceed)
                .map(([name, u]) => `- **${name}**: ${u.score}分`);
              
              body += '### 超额人员\n' + exceeds.join('\n');
            } catch(e) {
              body += '详情查看: ' + summaryPath;
            }
            
            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: 'exceed-alert',
              state: 'open'
            });
            
            const todayIssue = issues.data.find(i => i.title.includes(today));
            
            if (!todayIssue) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: `${title} - ${today}`,
                body,
                labels: ['exceed-alert', 'automated']
              });
            }
